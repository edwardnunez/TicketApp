# ==========================================================
# Ejemplo de configuración de entorno (.env.example)
# Proyecto: TicketApp
# ==========================================================
# Este archivo contiene las variables de entorno necesarias
# para ejecutar el sistema en entorno local o de desarrollo.
# Se recomienda copiarlo como ".env" y ajustar los valores
# según la configuración de cada máquina.
# ==========================================================

# ----------------------------------------------------------
# FRONTEND CONFIGURATION
# ----------------------------------------------------------

# Endpoint del API Gateway accesible desde el frontend React
REACT_APP_API_ENDPOINT=http://localhost:8000

# ID del cliente PayPal (modo sandbox o producción)
REACT_APP_PAYPAL_CLIENT_ID=YOUR_PAYPAL_CLIENT_ID_HERE

# Entorno de PayPal (sandbox o live)
REACT_APP_PAYPAL_ENVIRONMENT=sandbox


# ----------------------------------------------------------
# BACKEND SERVICES URLs
# ----------------------------------------------------------

# URL base de cada microservicio
USER_SERVICE_URL=http://localhost:8001
TICKET_SERVICE_URL=http://localhost:8002
EVENT_SERVICE_URL=http://localhost:8003
LOCATION_SERVICE_URL=http://localhost:8004

# URL del servicio gateway (usado para enrutar peticiones)
GATEWAY_SERVICE_URL=http://gatewayservice:8000


# ----------------------------------------------------------
# DATABASE CONNECTION STRINGS
# ----------------------------------------------------------

# URIs de conexión para las distintas bases de datos MongoDB
# (ajustar puertos y nombres de base de datos según entorno)
MONGODB_URI=mongodb://localhost:27017/userdb
MONGODB_URI_EVENT=mongodb://localhost:27017/eventdb
MONGODB_URI_LOCATION=mongodb://localhost:27017/locationdb
MONGODB_URI_TICKET=mongodb://localhost:27017/ticketdb
MONGODB_URI_SEATMAP=mongodb://localhost:27017/seatmapdb


# ----------------------------------------------------------
# EMAIL (SMTP) CONFIGURATION
# ----------------------------------------------------------

# Configuración del servidor SMTP para envío de correos
# En desarrollo, se recomienda usar cuentas temporales
# o servicios de prueba como Mailtrap.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=example@gmail.com
SMTP_PASS=your_app_password_here
SMTP_FROM=TicketApp <no-reply@ticketapp.com>